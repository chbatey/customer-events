# Example Spark/Cassandra application

Runs in stand alone mode. Only dependency is a locally running Cassandra.



Tech todo:
* Introduce Cassandra unit so the tests don't rely on a running Cassandra
* Load test with Gatling
